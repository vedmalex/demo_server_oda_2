# Create SubjectCourse
mutation createSubjectCourse($subjectCourse: createSubjectCourseInput!) {
  createSubjectCourse(input: $subjectCourse) {
    subjectCourse {
      node {
        id
      }
    }
  }
}
